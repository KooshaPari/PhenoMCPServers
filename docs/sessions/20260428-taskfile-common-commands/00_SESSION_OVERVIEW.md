# Session Overview

Goal: add a root Taskfile with common build, test, lint, and clean tasks for the Python repo.

Success criteria:
- `Taskfile.yml` exists at the repo root.
- Tasks reflect the detected language and existing tooling.
- The file validates with `task --list` and the branch is published.
