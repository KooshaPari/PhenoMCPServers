# Specifications

Scope:
- Provide the four common root tasks requested by the user.
- Keep the file language-aware for this repo's Python toolchain.

Acceptance criteria:
- `build` builds the package.
- `test` runs the test suite.
- `lint` runs Ruff.
- `clean` removes generated artifacts without touching source files.
