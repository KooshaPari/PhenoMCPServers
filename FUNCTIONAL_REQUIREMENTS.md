# Functional Requirements

The canonical functional requirements for `agent-user-status` live in
[`docs/FUNCTIONAL_REQUIREMENTS.md`](docs/FUNCTIONAL_REQUIREMENTS.md).

This root file is intentionally only a pointer so requirement IDs stay in one
namespace: `FR-AGENT_USER_STATUS-NNN`.
