name: Doc Links
on: [push, pull_request]

jobs:
  links:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate documentation links and metadata
        run: scripts/validate-docs.sh links
